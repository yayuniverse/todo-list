function createTodoList(
  title,
  priority
  // description,
  // notes,
  // checklist,
  // dueDate
) {
  return {
    title,
    priority,
    // description,
    // notes,
    // checklist,
    // dueDate,
  };
}

export { createTodoList };
